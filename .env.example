# === Rust Server (server/) ===
# HTTP port for API / WebSocket (HTTPS is automatically port + 1)
# LJC_PORT=17200
# Bind address
# LJC_BIND=0.0.0.0

# === Next.js (web/) ===
# Rust server URL (used by Next.js API routes to proxy requests)
# RUST_SERVER_URL=http://localhost:17200
